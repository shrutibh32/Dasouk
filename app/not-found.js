// This page will be displayed, if a user tries to open undefined url.

import PageNotFound from "@/components/404/PageNotFound";

const NotFound = () => {
  return <PageNotFound />;
};

export default NotFound;
